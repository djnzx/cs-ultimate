# CS229: Machine Learning — Dependency Graph

Stanford's CS229 is structured in five progressive blocks. Each block depends on the ones before it.
The graph below shows every topic and its prerequisite relationships for a fluent learning curve.

---

## Dependency Graph

```mermaid
flowchart TD
    %% ─────────────── PREREQUISITES ───────────────
    subgraph PRE["🔷 Prerequisites (before the course)"]
        LA["Linear Algebra\n(vectors, matrices, eigenvectors, SVD)"]
        MC["Multivariable Calculus\n(gradients, Jacobian, chain rule)"]
        PROB["Probability & Statistics\n(distributions, expectation, Bayes)"]
        PY["Python / NumPy\n(CS106A/B level)"]
    end

    %% ─────────────── PART 1: SUPERVISED LEARNING ───────────────
    subgraph SL["📗 Part 1 — Supervised Learning"]
        LR["Linear Regression\n(gradient descent, normal equations)"]
        PI["Probabilistic Interpretation\n(MLE, least squares as Gaussian MLE)"]
        LOGREG["Logistic Regression\n(Newton's method, cross-entropy)"]
        PERC["Perceptron"]
        GLM["Exponential Family &\nGeneralized Linear Models"]
        GEN["Generative Algorithms\n(GDA, Naïve Bayes, Laplace smoothing)"]
        SVM["Support Vector Machines\n(max-margin, dual, kernels)"]
        KER["Kernel Methods\n(RBF, polynomial, kernel trick)"]
        GP["Gaussian Processes"]
        NN["Neural Networks\n(backpropagation, activation functions)"]
        DL["Deep Learning\n(deep nets, batch norm, dropout)"]
        CNN["Convolutional Networks\n(conv layers, pooling, ResNet)"]
    end

    %% ─────────────── PART 2: BIAS-VARIANCE & LEARNING THEORY ───────────────
    subgraph LT["📘 Part 2 — Bias-Variance & Learning Theory"]
        BV["Bias-Variance Tradeoff\n(underfitting vs overfitting)"]
        REG["Regularization\n(L1/L2, Bayesian MAP interpretation)"]
        MS["Model Selection\n(cross-validation, train/dev/test splits)"]
        SLT["Statistical Learning Theory\n(VC dimension, uniform convergence)"]
    end

    %% ─────────────── PART 3: REINFORCEMENT LEARNING ───────────────
    subgraph RL["📕 Part 3 — Reinforcement Learning"]
        MDP["Markov Decision Processes\n(states, actions, rewards, discount)"]
        VI["Value Iteration &\nPolicy Iteration"]
        LMDP["Learning the MDP Model\n(model-based RL)"]
        CONT["Continuous States &\nApproximation Methods\n(fitted VI, linear quadratic)"]
    end

    %% ─────────────── PART 4: UNSUPERVISED LEARNING ───────────────
    subgraph UL["📙 Part 4 — Unsupervised Learning"]
        KM["K-Means Clustering"]
        MOG["Mixture of Gaussians\n(soft assignments)"]
        EM["Expectation-Maximization\n(E-step / M-step, ELBO)"]
        FA["Factor Analysis\n(latent factors, low-rank covariance)"]
        PCA["Principal Components Analysis\n(variance maximization, SVD)"]
        ICA["Independent Components Analysis\n(non-Gaussian sources)"]
    end

    %% ─────────────── PART 5: ADVANCED TOPICS ───────────────
    subgraph ADV["📓 Part 5 — Advanced Topics"]
        ME["Maximum Entropy &\nKL-Divergence"]
        VI2["Variational Inference\n(ELBO, mean-field)"]
        VAE["Variational Autoencoder"]
        DT["Decision Trees\n(CART, information gain)"]
        BOOST["Boosting & Ensemble Methods\n(AdaBoost, Gradient Boosting)"]
        EVAL["Evaluation Metrics\n(precision, recall, F1, AUC-ROC)"]
    end

    %% ═══════════════ EDGES ═══════════════

    %% Prerequisites → Supervised Learning
    LA  --> LR
    LA  --> SVM
    LA  --> PCA
    MC  --> LR
    MC  --> NN
    PROB --> PI
    PROB --> GEN
    PROB --> SVM
    PY  --> LR

    %% Supervised Learning internal chain
    LR   --> PI
    LR   --> PERC
    LR   --> BV
    PI   --> LOGREG
    LOGREG --> GLM
    LOGREG --> PERC
    GLM  --> GEN
    SVM  --> KER
    KER  --> GP
    LOGREG --> NN
    NN   --> DL
    DL   --> CNN
    DL   --> VAE

    %% Learning Theory
    LR   --> BV
    LOGREG --> BV
    BV   --> REG
    REG  --> MS
    MS   --> SLT

    %% Reinforcement Learning
    PROB --> MDP
    MDP  --> VI
    VI   --> LMDP
    LMDP --> CONT

    %% Unsupervised Learning internal chain
    PROB --> KM
    KM   --> MOG
    MOG  --> EM
    EM   --> FA
    LA   --> PCA
    FA   --> PCA
    PCA  --> ICA

    %% Advanced Topics
    PROB  --> ME
    EM    --> VI2
    ME    --> VI2
    VI2   --> VAE
    DL    --> VAE
    LR    --> EVAL
    LOGREG --> EVAL
    LOGREG --> DT
    DT    --> BOOST

    %% Cross-block bridges
    REG  --> EM
    SLT  --> BOOST
    NN   --> VAE
```

---

## Learning Curve: Recommended Order

| Step | Block | Topics |
|------|-------|--------|
| 0 | **Prerequisites** | Linear Algebra → Multivariable Calculus → Probability → Python/NumPy |
| 1 | **Supervised — Linear** | Linear Regression → Probabilistic Interpretation (MLE) → Logistic Regression → Perceptron → GLMs |
| 2 | **Supervised — Generative** | Generative Algorithms (GDA, Naïve Bayes, Laplace smoothing) |
| 3 | **Supervised — Kernel** | SVM → Kernel Methods → Gaussian Processes |
| 4 | **Supervised — Neural** | Neural Networks → Deep Learning → CNNs |
| 5 | **Learning Theory** | Bias-Variance → Regularization → Model Selection → Statistical Learning Theory |
| 6 | **Reinforcement Learning** | MDPs → Value/Policy Iteration → Model-Based RL → Continuous States |
| 7 | **Unsupervised** | K-Means → Mixture of Gaussians → EM → Factor Analysis → PCA → ICA |
| 8 | **Advanced** | Max Entropy / KL-Divergence → Variational Inference → VAE → Decision Trees → Boosting → Eval Metrics |

---

## Key Dependency Rules

- **Linear Algebra** is a hard prerequisite for Linear Regression, SVM, and PCA.
- **Probability** is a hard prerequisite for generative models, EM, and MDPs.
- **MLE / probabilistic interpretation** must come before Logistic Regression and GLMs.
- **EM** is the bridge between unsupervised learning and variational inference.
- **Neural Networks** must precede Deep Learning, CNNs, and VAEs.
- **Bias-Variance** must precede Regularization and Learning Theory.
- **MDPs + Value Iteration** must precede all model-based RL.

---

*Source: [CS229 Stanford](https://cs229.stanford.edu) — curriculum synthesized from the Spring 2026 homepage and the Summer 2019 public syllabus.*